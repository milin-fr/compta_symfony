{% extends 'base.html.twig' %}

{% block title %}WorkType{% endblock %}

{% block body %}
    <h1>WorkType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work_type.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ work_type.title }}</td>
            </tr>
            <tr>
                <th>BudgetEuro</th>
                <td>{{ work_type.budgetEuro }}</td>
            </tr>
            <tr>
                <th>BudgetCent</th>
                <td>{{ work_type.budgetCent }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('work_type_index') }}">back to list</a>

    <a href="{{ path('work_type_edit', {'id': work_type.id}) }}">edit</a>

    {{ include('work_type/_delete_form.html.twig') }}
{% endblock %}
