<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="{{ asset('css/bootstrap.min.css') }}">
            <link rel="stylesheet" href="{{ asset('css/style.css') }}">
        {% endblock %}
    </head>
    <body>
        <div class="container">
            <nav class="navbar navbar-light bg-light">
                <a href="{{ path('bill_new') }}">Nouvelle facture</a>
                <a href="{{ path('bill_index') }}">Liste des factures</a>
                <a href="{{ path('company_index') }}">Menu des entreprises</a>
                <a href="{{ path('work_type_index') }}">Menu des charges</a>
            </nav>
            {% block body %}{% endblock %}
        </div>
            <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
